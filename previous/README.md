# previous

A light **legacy showcase** — previous & weird GCU designs, shown as "things we made," running from
**`endarthur.github.io`** (the origin) through the ancestor instruments to the GCU. Mostly single-file /
offline artifacts, so the showcase can embed the real things live.

**Only already-public work lives here.** Personal-only experiments are kept out of the public tree by
construction (gitignored `_personal/`).

Currently archived: `index-mock.html` (a superseded landing mock).
